// function hello(name = 'Tom') {
//   console.log('hello' + name);
// }
const hello = (name, age) => 40;
// (name = 'Tom')のようにデフォルト値が設定されている場合は、()は消せないが、デフォルト値がない場合は、()も削除できる。
// => の先が1行しかない場合は、{ }は削除できる。関数の引数が2つ以上の場合は、()が必要。

  const arry = [1, 2, 3, 4, 5, 6, 7, 8, 9];

  arry.forEach(value => console.log(value));
